import React from 'react';
import Options from '../components/Options';
import Timetable from '../components/Timetable';

const Teachers =  () => {
    
    return (
        <div>
           <Options type="teachers"></Options>
           <Timetable type="teachers"></Timetable>
        </div>
    );
};

export default Teachers;

