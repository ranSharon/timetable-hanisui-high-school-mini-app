import React from 'react';
import Options from '../components/Options';
import Timetable from '../components/Timetable';

const ClassRooms = () => {

    return (
        <div>
            <Options type="classRooms"></Options>
            <Timetable type="classRooms"></Timetable>
        </div>
    );
};

export default ClassRooms;
